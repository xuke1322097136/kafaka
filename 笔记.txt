Kafka消费模式：相对于点对点（一对一）的模式来讲，它是基于发布/订阅模式（一对多）的，也就是说，消息是可以传给多个消费者的。
     第二，它是发布订阅模式中的基于拉取这种方式的处理方式，消费者的消费速度可以由消费者自己决定，
	 而不是简单的推送这种方式，因为每个消费者的消息消费能力都很有可能是不一样的。而这种拉取的方式
	 有一个明显的缺点，就是在消费者这边需要维护一个长轮询，需要去询问消息队列中有没有新消息，有时候会比较浪费资源。而基于推送的这种
	 方式有新的消息的话，就直接推送给你了，并不需要你主动去获取。
	
Kafka简单架构：